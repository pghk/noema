language: ruby

addons:
  apt:
    packages:
      - libcurl4-openssl-dev # required to avoid SSL errors

before_install:
  - export NOKOGIRI_USE_SYSTEM_LIBRARIES=true

install:
  - gem install html-proofer

script:
  - htmlproofer ./dist $(tests/html-args.sh)